import runtime;
import string;

fib(n: int, head: [int]) -> [int] {
    arraySize = length(head);
    if (arraySize == n) head
    else if (arraySize == 0) fib(n, [0])
    else if (arraySize == 1) fib(n, [0, 1])
    else {
        fib(n, concat(head, [head[arraySize-2] + head[arraySize-1]]));
    }
    
}


main() {
    println(fib(8, []));
    quit(0);
}