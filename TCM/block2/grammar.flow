export {
    MyExpr ::= Dbl, Var, Sum, Sub, Mul, Div, Pow, Neg;
    Dbl : (val : double);
    Var : (val : string);
    Sum : (l : MyExpr, r : MyExpr);
    Sub : (l : MyExpr, r : MyExpr);
    Mul : (l : MyExpr, r : MyExpr);
    Div : (l : MyExpr, r : MyExpr);
    Pow : (l : MyExpr, r : MyExpr);
    Neg : (e : MyExpr);
}