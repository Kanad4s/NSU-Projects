export {
    MyExpr ::= Int, Sum, Sub, Mul, Div;
    Int : (val : int);
    Sum : (l : MyExpr, r : MyExpr);
    Sub : (l : MyExpr, r : MyExpr);
    Mul : (l : MyExpr, r : MyExpr);
    Div : (l : MyExpr, r : MyExpr);
}