name: Test ssh CD

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Setup SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/booktok
        chmod 600 ~/booktok
    - name: Add server to known hosts
      run: |
        ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

    # - name: Debug SSH key format
    #   run: |
    #     head -n 2 ~/.ssh/id_ed25519
    #     tail -n 2 ~/.ssh/id_ed25519

    - name: Test SSH connection
      run: |
        ssh -o StrictHostKeyChecking=no -i ~/booktok ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} "echo 'SSH works!!'"
